const polish=document.createElement('link');polish.rel='stylesheet';polish.href='production.css';document.head.appendChild(polish);const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav');const header=document.querySelector('.site-header');const setMenu=(open)=>{if(!nav||!toggle)return;nav.classList.toggle('open',open);toggle.classList.toggle('active',open);toggle.setAttribute('aria-expanded',String(open));toggle.setAttribute('aria-label',open?'Close menu':'Open menu');document.body.classList.toggle('menu-open',open)};toggle?.addEventListener('click',()=>setMenu(!nav?.classList.contains('open')));document.querySelectorAll('.nav a').forEach(link=>link.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',event=>{if(event.key==='Escape')setMenu(false)});const updateHeader=()=>header?.classList.toggle('scrolled',window.scrollY>40);updateHeader();window.addEventListener('scroll',updateHeader,{passive:true});const revealItems=document.querySelectorAll('.intro,.values,.menu-preview,.chef,.gallery,.experience,.private-dining,.social-cta,.reserve');if('IntersectionObserver'in window&&!window.matchMedia('(prefers-reduced-motion: reduce)').matches){revealItems.forEach(item=>item.classList.add('reveal'));const observer=new IntersectionObserver((entries,instance)=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');instance.unobserve(entry.target)}})},{threshold:.12});revealItems.forEach(item=>observer.observe(item))}const sections=[...document.querySelectorAll('main section[id]')];const navLinks=[...document.querySelectorAll('.nav a[href^="#"]')];if('IntersectionObserver'in window&&sections.length&&navLinks.length){const sectionObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;navLinks.forEach(link=>link.classList.toggle('active',link.getAttribute('href')===`#${entry.target.id}`))})},{rootMargin:'-35% 0px -55% 0px',threshold:0});sections.forEach(section=>sectionObserver.observe(section))}const tabs=[...document.querySelectorAll('.menu-tab')];const panels=[...document.querySelectorAll('.menu-panel')];const activateMenu=(key,moveFocus=false)=>{tabs.forEach(tab=>{const active=tab.dataset.menu===key;tab.classList.toggle('is-active',active);tab.classList.toggle('active',active);tab.setAttribute('aria-selected',String(active));tab.tabIndex=active?0:-1});panels.forEach(panel=>{const active=panel.id===`menu-panel-${key}`;panel.hidden=!active;panel.classList.toggle('active',active)});if(moveFocus)tabs.find(tab=>tab.dataset.menu===key)?.focus()};tabs.forEach((tab,index)=>{tab.addEventListener('click',()=>activateMenu(tab.dataset.menu));tab.addEventListener('keydown',event=>{if(!['ArrowRight','ArrowLeft','Home','End'].includes(event.key))return;event.preventDefault();let next=index;if(event.key==='ArrowRight')next=(index+1)%tabs.length;if(event.key==='ArrowLeft')next=(index-1+tabs.length)%tabs.length;if(event.key==='Home')next=0;if(event.key==='End')next=tabs.length-1;activateMenu(tabs[next].dataset.menu,true)})});const isHome=location.pathname.endsWith('/')||location.pathname.endsWith('/index.html');if(isHome&&nav){const pageLinks=[['our-story.html','Our Story'],['menu.html','Menu'],['chef-jomon.html','Chef Jomon'],['gallery.html','Gallery'],['private-dining.html','Private Dining']];nav.querySelectorAll('a').forEach((link,i)=>{if(i<pageLinks.length){link.href=pageLinks[i][0];link.textContent=pageLinks[i][1];link.classList.remove('nav-cta')}});const cta=nav.querySelector('a:last-child');if(cta){cta.href='reservations.html';cta.textContent='Reserve a Table';cta.classList.add('nav-cta')}}